#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    int score[3];                   // Uninitialize value of score array
    for (int i = 0; i < 3; i++)
    {
        printf("%i\n", score[i]);   // Random values
    }
}